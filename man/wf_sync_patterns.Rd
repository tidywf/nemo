% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{wf_sync_patterns}
\alias{wf_sync_patterns}
\title{S3 Sync Patterns for a Workflow}
\usage{
wf_sync_patterns(workflow)
}
\arguments{
\item{workflow}{(\code{character(1)})\cr
Workflow name (e.g. \code{"wigits"}, \code{"dragen"}), as accepted by
\code{\link[=nemoverse_wf_dispatch]{nemoverse_wf_dispatch()}}.}
}
\value{
(\code{tibble()})\cr
Tibble with \code{inex} (\code{"in"}/\code{"ex"}) and \code{pat} columns, suitable for
\code{\link[=s3sync]{s3sync()}}'s \code{pats} argument.
}
\description{
Builds the \verb{aws s3 sync} include/exclude pattern tibble for a workflow,
from the \code{glob} fields declared by each of its tool's \code{schema.yaml}.
This is the single source of truth for "which files does this workflow need?"
Downstream callers (\code{tidywigits::s3sync()}, \code{tidydragen::s3sync()}, the nemo
Lambda) should use it rather than maintaining their own copies.
}
\examples{
(pats <- wf_sync_patterns("workflow1"))
}
