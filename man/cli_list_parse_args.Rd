% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli_list.R
\name{cli_list_parse_args}
\alias{cli_list_parse_args}
\title{Parse and dispatch the 'list' subcommand}
\usage{
cli_list_parse_args(args, wf = NULL)
}
\arguments{
\item{args}{Named list of parsed CLI arguments, as returned by argparse.
Expected fields: \code{format}, \code{in_dir}, \code{workflow} (may be \code{NULL} when \code{wf}
is provided), \code{max}, \code{quiet}.}

\item{wf}{(\code{character(1)} or \code{NULL})\cr
Workflow override. When non-NULL, replaces \code{args$workflow}.}
}
\description{
Assembles arguments from the parsed argparse result and calls
\code{\link[=cli_nemo_list]{cli_nemo_list()}}. When \code{wf} is non-NULL it overrides \code{args$workflow}
(used by downstream packages that fix the workflow at the script level,
e.g. \code{tidywigits.R}).
}
\examples{
path <- system.file("extdata/tool1", package = "nemo")
args <- list(format = "pretty", in_dir = path, workflow = NULL, quiet = FALSE)
capture.output(cli_list_parse_args(args, wf = "workflow1"))
}
