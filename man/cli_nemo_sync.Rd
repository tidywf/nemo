% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli_sync.R
\name{cli_nemo_sync}
\alias{cli_nemo_sync}
\title{Sync parsable workflow output files from S3}
\usage{
cli_nemo_sync(src, dest, workflow, dryrun = FALSE, show_patterns = FALSE)
}
\arguments{
\item{src}{(\code{character(1)})\cr S3 source path.}

\item{dest}{(\code{character(1)})\cr Local destination directory.}

\item{workflow}{(\code{character(1)})\cr Workflow name (e.g. \code{"wigits"}).}

\item{dryrun}{(\code{logical(1)})\cr Pass \code{--dryrun} to \verb{aws s3 sync}.}

\item{show_patterns}{(\code{logical(1)})\cr
Print the include/exclude patterns as TSV to stdout and return without
syncing.}
}
\description{
Runs \verb{aws s3 sync} from \code{src} to \code{dest}, pulling down only the files the
workflow's tool schemas declare (see \code{\link[=wf_sync_patterns]{wf_sync_patterns()}}). This keeps the
"what do we need?" list in one place (the \code{schema.yaml} files) instead of
duplicating it in every caller.
}
\examples{
out <- capture.output(
  cli_nemo_sync(
    src = "s3://bucket/run1", dest = tempfile(),
    workflow = "workflow1", show_patterns = TRUE
  )
)
}
