% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{read_parquet_grep}
\alias{read_parquet_grep}
\title{Read Parquet File Matched By Pattern}
\usage{
read_parquet_grep(odir, lf, pattern, first = FALSE)
}
\arguments{
\item{odir}{(\code{character(1)})\cr
Directory the files live in.}

\item{lf}{(\code{character(n)})\cr
Basenames to match \code{pattern} against.}

\item{pattern}{(\code{character(1)})\cr
Regex passed to \code{grep()}.}

\item{first}{(\code{logical(1)})\cr
If \code{pattern} matches more than one file, read the first match instead of
erroring.}
}
\value{
(\code{tibble}) Parsed parquet file.
}
\description{
Test helper: reads the single parquet file in \code{odir} whose basename (from
\code{lf}) matches \code{pattern}. Errors on zero or multiple matches unless
\code{first = TRUE}.
}
\examples{
tmp <- tempfile(fileext = ".parquet")
arrow::write_parquet(data.frame(x = 1L), tmp)
(x <- read_parquet_grep(dirname(tmp), basename(tmp), basename(tmp)))
}
