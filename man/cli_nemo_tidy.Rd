% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli_tidy.R
\name{cli_nemo_tidy}
\alias{cli_nemo_tidy}
\title{Tidy workflow output files}
\usage{
cli_nemo_tidy(
  workflow,
  in_dir,
  out_dir,
  out_format,
  input_id = NULL,
  output_id = NULL,
  pfix_include = FALSE,
  dbname,
  dbuser,
  include,
  exclude
)
}
\arguments{
\item{workflow}{(\code{character(1)})\cr Workflow name passed to
\code{\link[=nemoverse_wf_dispatch]{nemoverse_wf_dispatch()}}.}

\item{in_dir}{(\code{character(1)})\cr Input directory to search.}

\item{out_dir}{(\code{character(1)} or \code{NULL})\cr Output directory. Required
unless \code{out_format} is \code{"db"}.}

\item{out_format}{(\code{character(1)})\cr Output format. One of \code{"parquet"},
\code{"tsv"}, \code{"csv"}, \code{"rds"}, or \code{"db"}.}

\item{input_id}{(\code{character(1)} or \code{NULL})\cr Input run identifier.}

\item{output_id}{(\code{character(1)} or \code{NULL})\cr Output run identifier.}

\item{pfix_include}{(\code{logical(1)})\cr
If \code{TRUE}, prepend an \code{input_pfix} column to each tidy table.}

\item{dbname}{(\code{character(1)} or \code{NULL})\cr Database name. Required when
\code{out_format} is \code{"db"}.}

\item{dbuser}{(\code{character(1)} or \code{NULL})\cr Database user. Required when
\code{out_format} is \code{"db"}.}

\item{include}{(\code{character(n)} or \code{NULL})\cr Tool parser names to include.
\code{NULL} includes all.}

\item{exclude}{(\code{character(n)} or \code{NULL})\cr Tool parser names to exclude.
\code{NULL} excludes none.}
}
\description{
Discovers and tidies files under \code{in_dir} for the given workflow, writing
results to \code{out_dir} in the requested format.
}
\examples{
path <- system.file("extdata/tool1", package = "nemo")
out <- tempfile()
res <- cli_nemo_tidy(
  workflow = "workflow1", in_dir = path, out_dir = out,
  out_format = "parquet", input_id = "run1", output_id = NULL,
  dbname = NULL, dbuser = NULL, include = NULL, exclude = NULL
)
}
