% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli_sync.R
\name{cli_sync_parse_args}
\alias{cli_sync_parse_args}
\title{Parse and dispatch the 'sync' subcommand}
\usage{
cli_sync_parse_args(args, wf = NULL)
}
\arguments{
\item{args}{Named list of parsed CLI arguments, as returned by argparse.
Expected fields: \code{src}, \code{dest}, \code{workflow} (may be \code{NULL} when \code{wf} is
provided), \code{dryrun}, \code{show_patterns}, \code{quiet}.}

\item{wf}{(\code{character(1)} or \code{NULL})\cr
Workflow override. When non-NULL, replaces \code{args$workflow}.}
}
\description{
Assembles arguments from the parsed argparse result and calls
\code{\link[=cli_nemo_sync]{cli_nemo_sync()}}. When \code{wf} is non-NULL it overrides \code{args$workflow}
(used by downstream packages that fix the workflow at the script level,
e.g. \code{tidywigits.R}).
}
\examples{
args <- list(
  src = "s3://bucket/run1", dest = tempfile(), workflow = NULL,
  dryrun = FALSE, show_patterns = TRUE, quiet = FALSE
)
out <- capture.output(cli_sync_parse_args(args, wf = "workflow1"))
}
