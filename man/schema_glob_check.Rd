% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{schema_glob_check}
\alias{schema_glob_check}
\title{Check Schema Globs Against Schema Patterns}
\usage{
schema_glob_check(pkg, fixture_dir = NULL)
}
\arguments{
\item{pkg}{(\code{character(1)})\cr
Package name whose schemas to check.}

\item{fixture_dir}{(\code{character(1)})\cr
Directory of test fixtures to match against. Defaults to the package's
\code{extdata}.}
}
\value{
(\code{tibble()})\cr
One row per (tool, table, file) that \code{pattern} matches but no \code{glob} does.
Zero rows means the schemas are consistent.
}
\description{
Guards the \code{glob} fields in a package's \code{schema.yaml} files against drifting
away from their \code{pattern} fields. For every table, each test-fixture file
that \code{pattern} matches must also be matched by at least one \code{glob} —
otherwise that file would never be synced down and the parser would silently
see nothing.
}
\details{
Only the too-narrow direction is checked: a \code{glob} wider than its \code{pattern}
merely syncs a few extra files, which the parsers ignore.

\code{pattern} is matched against basenames (as \code{Tool} does when discovering
files) while \code{glob} is matched against the path relative to \code{fixture_dir}
(as \verb{aws s3 sync} matches keys relative to the sync root), so a glob may be
tightened with a directory prefix without failing this check.
}
\examples{
(bad <- schema_glob_check("nemo"))
}
