% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli_tidy.R
\name{cli_tidy_parse_args}
\alias{cli_tidy_parse_args}
\title{Parse and dispatch the 'tidy' subcommand}
\usage{
cli_tidy_parse_args(args, wf = NULL, dbdrv = NULL)
}
\arguments{
\item{args}{Named list of parsed CLI arguments, as returned by argparse.
Expected fields: \code{format}, \code{in_dir}, \code{output_dir}, \code{input_id} (optional),
\code{output_id} (optional), \code{ulid}, \code{prefix_include}, \code{dbname}, \code{dbuser},
\code{include}, \code{exclude}, \code{workflow} (may be \code{NULL} when \code{wf} is provided),
\code{quiet}.
\code{output_id} and \code{ulid} are mutually exclusive at the CLI level (enforced by
argparse). When called directly, if both are set \code{ulid} takes precedence
and \code{output_id} is silently ignored.}

\item{wf}{(\code{character(1)} or \code{NULL})\cr
Workflow override. When non-NULL, replaces \code{args$workflow}.}

\item{dbdrv}{(\code{DBIDriver} or \code{NULL})\cr
DBI driver object (e.g. \code{RPostgres::Postgres()}). Required when
\code{args$format} is \code{"db"}; ignored otherwise. Supplied by the caller so that
nemo does not depend on any specific database backend.}
}
\description{
Validates and normalises arguments from the parsed argparse result, then
calls \code{\link[=cli_nemo_tidy]{cli_nemo_tidy()}}. When \code{wf} is non-NULL it overrides \code{args$workflow}
(used by downstream packages that fix the workflow at the script level,
e.g. \code{tidywigits.R}).
}
\examples{
path <- system.file("extdata/tool1", package = "nemo")
args <- list(
  format = "tsv", in_dir = path, output_dir = tempfile(),
  input_id = "run1", output_id = NULL, ulid = FALSE,
  prefix_include = FALSE, workflow = NULL, dbname = NULL, dbuser = NULL,
  include = NULL, exclude = NULL, quiet = FALSE
)
cli_tidy_parse_args(args, wf = "workflow1")
}
